/* ─────────────────────────────────────────────────────────────────────
   GUIDEON 공통 컴포넌트 스타일
   ───────────────────────────────────────────────────────────────────── */

/* ── 화면 루트 ── */
.screen-root {
    width: 1920px;
    height: 1080px;
    background-color: rgb(245, 237, 224);
    position: absolute;
    overflow: hidden;
    flex-direction: column;
    -unity-font: url("project://database/Assets/_Project/Font/Noto/NotoSansKR-Regular.ttf");
}

/* ── 단청 띠 (상/하단) ── */
.dancheong-band {
    width: 100%;
    height: 12px;
    background-color: rgb(200, 64, 48);
    flex-direction: row;
    flex-shrink: 0;
}

.dancheong-band--bottom {
    /* 하단 단청 띠: 별도 마진 없이 화면 끝에 붙음 */
}

/* ── 창살 배경 (전체 화면에 SVG 배경 타일) ── */
.changsal-bg {
    /* SVG 배경은 C#에서 동적으로 로드해 적용.
       Unity UI Toolkit background-image + background-repeat: repeat 사용 */
    background-color: rgb(245, 237, 224);
}

/* ── 헤더 바 ── */
.header-bar {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    height: 76px;
    padding: 0 56px;
    background-color: rgba(245, 237, 224, 0.97);
    border-bottom-width: 1px;
    border-bottom-color: rgba(90, 60, 20, 0.12);
    flex-shrink: 0;
}

/* ── 로고 그룹 ── */
.logo-group {
    flex-direction: row;
    align-items: center;
}

.logo-wordmark {
    font-size: 22px;
    color: rgb(26, 18, 8);
    letter-spacing: 2px;
    -unity-font-style: bold;
}

.logo-sub {
    font-size: 11px;
    color: rgb(138, 112, 96);
    letter-spacing: 4px;
    margin-top: 2px;
}

/* ── 칩 (운영 정보, 빠른 질문 등) ── */
.chip {
    flex-direction: row;
    align-items: center;
    padding: 10px 24px;
    background-color: rgba(255, 252, 245, 0.8);
    border-width: 1px;
    border-color: rgba(90, 60, 20, 0.12);
    border-radius: 40px;
}

.chip__label {
    font-size: 11px;
    color: rgb(138, 112, 96);
    letter-spacing: 2px;
}

.chip__value {
    font-size: 16px;
    color: rgb(26, 18, 8);
    -unity-font-style: bold;
}

/* ── 카드 ── */
.card {
    background-color: rgba(255, 252, 245, 0.92);
    border-width: 1px;
    border-color: rgba(90, 60, 20, 0.15);
    border-radius: 16px;
}

.card--lg {
    border-radius: 24px;
}

/* ── 말풍선 ── */
.bubble-user {
    align-self: flex-end;
    max-width: 680px;
    background-color: rgba(200, 64, 48, 0.08);
    border-width: 1.5px;
    border-color: rgba(200, 64, 48, 0.2);
    border-radius: 26px 6px 26px 26px;
    padding: 22px 32px;
}

.bubble-ai {
    align-self: flex-start;
    max-width: 680px;
    background-color: rgba(255, 252, 245, 0.96);
    border-width: 1.5px;
    border-color: rgba(90, 60, 20, 0.12);
    border-radius: 6px 26px 26px 26px;
    padding: 24px 32px;
}

.bubble__label {
    font-size: 13px;
    color: rgba(200, 64, 48, 0.5);
    letter-spacing: 3px;
    margin-bottom: 10px;
}

.bubble__text {
    font-size: 26px;
    color: rgb(26, 18, 8);
    white-space: normal;
    -unity-text-align: upper-left;
}

/* ── 마스코트 슬롯 ── */
.mascot-slot {
    width: 640px;
    height: 640px;
    /* Step 9에서 RenderTexture RawImage로 채울 영역 */
    /* 시각적 placeholder — C#에서 배경 색으로 표시 */
    background-color: rgba(200, 64, 48, 0.03);
    border-radius: 9999px;
    flex-shrink: 0;
}

.mascot-slot--sm {
    width: 320px;
    height: 320px;
}

/* ── AI 아바타 G 원 ── */
.ai-avatar {
    width: 56px;
    height: 56px;
    border-radius: 9999px;
    background-color: rgba(200, 64, 48, 0.07);
    border-width: 2px;
    border-color: rgb(177, 74, 46);
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.ai-avatar__text {
    font-size: 24px;
    color: rgb(177, 74, 46);
    -unity-font-style: bold;
}

/* ── 마이크 입력 바 ── */
.mic-bar {
    flex-direction: row;
    align-items: center;
    padding: 20px 28px;
    background-color: rgba(200, 64, 48, 0.05);
    border-width: 2px;
    border-color: rgba(200, 64, 48, 0.18);
    border-radius: 20px;
    margin-bottom: 16px;
}

.mic-circle {
    width: 56px;
    height: 56px;
    border-radius: 9999px;
    background-color: rgba(200, 64, 48, 0.1);
    border-width: 2px;
    border-color: rgb(177, 74, 46);
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.mic-hint {
    font-size: 17px;
    color: rgb(74, 53, 32);
    -unity-font-style: italic;
    margin-left: 16px;
    flex-shrink: 0;
}

/* ── 파형 ── */
.waveform-container {
    flex-direction: row;
    align-items: center;
    flex: 1;
    height: 40px;
    padding: 0 16px;
}

.waveform-bar {
    width: 5px;
    border-radius: 3px;
    background-color: rgba(200, 64, 48, 0.35);
    margin-right: 3px;
    align-self: center;
}

/* ── 빠른 질문 칩 ── */
.quick-chip {
    padding: 12px 24px;
    border-radius: 28px;
    background-color: rgba(255, 252, 245, 0.92);
    border-width: 1px;
    border-color: rgba(90, 60, 20, 0.12);
    font-size: 17px;
    color: rgb(74, 53, 32);
    margin-right: 8px;
    margin-bottom: 8px;
}

.quick-chip:hover {
    background-color: rgba(200, 64, 48, 0.06);
    border-color: rgba(200, 64, 48, 0.2);
}

/* ── 버튼 (헤더 액션) ── */
.action-btn {
    padding: 8px 22px;
    border-radius: 20px;
    background-color: rgba(200, 64, 48, 0.05);
    border-width: 1px;
    border-color: rgba(200, 64, 48, 0.13);
    color: rgb(74, 53, 32);
    font-size: 15px;
}

.action-btn--danger {
    background-color: transparent;
    border-width: 1.5px;
    border-color: rgba(200, 64, 48, 0.4);
    color: rgb(177, 74, 46);
    -unity-font-style: bold;
}

/* ── AI 안내 상태 점 ── */
.status-dot {
    width: 9px;
    height: 9px;
    border-radius: 9999px;
    background-color: rgb(92, 184, 92);
    margin-right: 8px;
    flex-shrink: 0;
}

/* ── 타임아웃 칩 ── */
.timeout-chip {
    flex-direction: row;
    align-items: center;
    padding: 10px 20px;
    border-radius: 20px;
    background-color: rgba(255, 252, 245, 0.85);
    border-width: 1px;
    border-color: rgba(90, 60, 20, 0.12);
}

.timeout-bar {
    height: 3px;
    border-radius: 4px;
    background-color: rgba(90, 60, 20, 0.08);
    margin-top: 8px;
}

.timeout-bar__fill {
    height: 3px;
    border-radius: 4px;
    background-color: rgb(177, 74, 46);
    transition: width 1s;
}

/* ── 구분 화살표 ── */
.divider-arrow {
    flex-direction: row;
    align-items: center;
    margin: 16px 0;
}

.divider-line {
    flex: 1;
    height: 1px;
    background-color: rgba(90, 60, 20, 0.07);
}

/* ── 글로벌 폰트 폴백 — UI Toolkit Label 이 기본 폰트 없이 invisible 되는 현상 방지 ── */
Label {
    -unity-font: url("project://database/Assets/_Project/Font/Noto/NotoSansKR-Regular.ttf");
}

Button {
    -unity-font: url("project://database/Assets/_Project/Font/Noto/NotoSansKR-Regular.ttf");
}

/* ── Hidden 유틸 ── */
.hidden {
    display: none;
}

/* ── 텍스트 스타일 ── */
.text-ink        { color: rgb(26, 18, 8); }
.text-ink-mid    { color: rgb(74, 53, 32); }
.text-ink-light  { color: rgb(138, 112, 96); }
.text-accent     { color: rgb(177, 74, 46); }
.text-bold       { -unity-font-style: bold; }
.text-light      { -unity-font-style: normal; }


